# Bases


1. &#32;
```c
if (!(i < 8) && !(i > 8)) then
    printf("i vaut 8\n");
```

1. &#32;
```c
if (!(i < 8) && !(i > 8))
    printf("i vaut 8");
    printf("\n");
```

1. &#32;
```c
if !(i < 8) && !(i > 8)
    printf("i vaut 8\n");
```
1. &#32;
```c
if (!(i < 8) && !(i > 8))
    printf("i vaut 8\n");
```

1. &#32;
```c
if (i = 8) printf("i vaut 8\n");
```

1. &#32;
```c
if (i & (1 << 3)) printf("i vaut 8\n");
```

1. &#32;
```c
if (i ^ 8) printf("i vaut 8\n");
```

1. &#32;
```c
if (i - 8) printf("i vaut 8\n");
```

1. &#32;
```c
if (i == 1 << 3) printf ("i vaut 8\n");
```

1.  &#32;
```c
if (!((i < 8) || (i > 8)))
    printf("i vaut 8\n");
```