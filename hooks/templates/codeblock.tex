\begin{code}{\VAR{language|default('text')}}{\VAR{filename|default('')}}{
    \BLOCK{- if linenos}linenos \BLOCK{endif -}
    \BLOCK{- if highlight }, highlightlines={\VAR{','.join(highlight)}}\BLOCK{endif -}
}
\VAR{code}
\end{code}
