\BLOCK{- set style = {
    'left': 'l',
    'right': 'r',
    'center': 'c',
    'justify': 'p'
} -}
\begin{tabular}{\BLOCK{- for s in columns}\VAR{style[column]}\BLOCK{- endfor}}
\toprule
\BLOCK{- for row in rows -}
\BLOCK{- if loop.first -}
\BLOCK{- for col in row -}
   \textbf{\VAR{col}}\BLOCK{- if not loop.last -} & \BLOCK{- else -} \\\BLOCK{- endif}
\BLOCK{- endfor -}
\middlerule
\BLOCK{- else -}
\BLOCK{- for col in row -}
   \VAR{col}\BLOCK{- if not loop.last -}&\BLOCK{- else -}\\\BLOCK{- endif}
\BLOCK{- endfor -}
\BLOCK{- endif -}
\BLOCK{- endfor -}
\bottomrule
\end{tabular}
